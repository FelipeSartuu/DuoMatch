<div>
    <a href="/posts/meusposts">Voltar</a>
    <h1>Editando o Post</h1>

    <form action="/posts/edit" method="POST">
    <input type="hidden" name="id" value="{{post.id}}">
        <div class="form-control">
            <label for="title">Título</label>
            <input type="text" name="title" placeholder="Digite um título" value="{{post.title}}">
        </div>

        <div class="form-control">
            <label for="game">Jogo</label>
            <input type="text" name="game" placeholder="Digite o nome do jogo" value="{{post.game}}">
        </div>

        <div class="form-control">
            <label for="horario">Disponibilidade</label>
            <input type="text" name="horario" placeholder="Digite sua disponibilidade para jogar" value="{{post.horario}}">
        </div>

        <div class="form-control">
            <label for="desc">Descrição</label>
            <input type="text" name="desc" placeholder="Crie uma descrição para a postagem" value="{{post.desc}}">
        </div>

        <input type="submit" value="Editar post">
    </form>
</div>